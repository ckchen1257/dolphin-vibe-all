services:
  dolphin-vibe-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: dolphin-vibe-server:local
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080